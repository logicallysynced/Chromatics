<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Chromatics.ViewModels"
             xmlns:loc="clr-namespace:Chromatics.Localization"
             mc:Ignorable="d" d:DesignWidth="1200" d:DesignHeight="720"
             x:Class="Chromatics.Views.PaletteView"
             x:DataType="vm:PaletteViewModel">

    <Grid ColumnDefinitions="*,16,420" Margin="16">

        <!-- Left: filter + grid + buttons -->
        <Grid Grid.Column="0" RowDefinitions="Auto,8,*,8,Auto">
            <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
                <TextBlock Text="{loc:Tr Key='Filter:'}" VerticalAlignment="Center" />
                <ComboBox ItemsSource="{Binding Categories}"
                          SelectedItem="{Binding SelectedCategory, Mode=TwoWay}"
                          MinWidth="220" />
            </StackPanel>

            <Border Grid.Row="2"
                    BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"
                    BorderThickness="1"
                    CornerRadius="4">
                <DataGrid ItemsSource="{Binding Items}"
                          SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          CanUserResizeColumns="True"
                          CanUserSortColumns="True"
                          HeadersVisibility="Column"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{loc:Tr Key=Name}"     Binding="{Binding DisplayName}" Width="*" />
                        <DataGridTextColumn Header="{loc:Tr Key=Category}" Binding="{Binding CategoryName}" Width="180" />
                        <!-- Right margin keeps the swatch clear of the 14px vertical scrollbar. -->
                        <DataGridTemplateColumn Header="{loc:Tr Key=Color}" Width="90" CanUserSort="False">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate DataType="vm:PaletteMappingItem">
                                    <Border Background="{Binding Brush}"
                                            CornerRadius="4"
                                            BorderBrush="#33000000"
                                            BorderThickness="1"
                                            Margin="6,4,18,4" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <StackPanel Grid.Row="4" Orientation="Horizontal" Spacing="8">
                <Button Content="{loc:Tr Key=Import}"
                        Click="OnImportClick"
                        ToolTip.Tip="{loc:Tr Key='Import Chromatics color palette from file'}" />
                <Button Content="{loc:Tr Key=Export}"
                        Click="OnExportClick"
                        ToolTip.Tip="{loc:Tr Key='Export Chromatics color palette to file'}" />
                <Button Content="{loc:Tr Key='Restore Default'}"
                        Command="{Binding UndoCommand}"
                        ToolTip.Tip="{loc:Tr Key='Restore selected color mapping to its default'}" />
            </StackPanel>
        </Grid>

        <!-- Right: editor + preview -->
        <Border Grid.Column="2"
                BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12">
            <Grid RowDefinitions="Auto,8,Auto,8,*">

                <!-- Avalonia's FallbackValue can't evaluate a nested markup
                     extension, so a {loc:Tr} fallback gets stringified as
                     "Avalonia.Data.Binding". Use the same visibility-toggle
                     pattern as the color-picker overlay below. -->
                <TextBlock Grid.Row="0"
                           Text="{Binding SelectedItem.DisplayName}"
                           IsVisible="{Binding EditorEnabled}"
                           FontSize="16"
                           FontWeight="SemiBold"
                           TextWrapping="Wrap" />
                <TextBlock Grid.Row="0"
                           Text="{loc:Tr Key='Select a mapping to edit'}"
                           IsVisible="{Binding !EditorEnabled}"
                           FontSize="16"
                           FontWeight="SemiBold"
                           TextWrapping="Wrap"
                           Opacity="0.6" />

                <Border Grid.Row="2"
                        Height="40"
                        CornerRadius="4"
                        BorderBrush="#33000000"
                        BorderThickness="1"
                        Background="{Binding SelectedItem.Brush, FallbackValue=Transparent}" />

                <!-- Color picker with placeholder overlay when nothing selected -->
                <Panel Grid.Row="4">
                    <ColorView Color="{Binding EditorColor, Mode=TwoWay}"
                               IsEnabled="{Binding EditorEnabled}"
                               IsAlphaEnabled="False"
                               IsAlphaVisible="False" />

                    <!-- Overlay shown when editor is disabled -->
                    <Border Background="{DynamicResource SystemControlPageBackgroundAltHighBrush}"
                            CornerRadius="4"
                            Opacity="0.55"
                            IsVisible="{Binding !EditorEnabled}" />
                    <TextBlock Text="{loc:Tr Key='Select a color mapping from the list to edit'}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               TextAlignment="Center"
                               Opacity="0.6"
                               IsVisible="{Binding !EditorEnabled}" />
                </Panel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
